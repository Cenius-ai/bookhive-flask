# Flask secret key — generate a random string for production
# In dev, a random key is generated automatically if this is unset.
SECRET_KEY=cenius-dev-a1b2c3d4e5f67890

# Database URL (SQLite by default, no need to change for local dev)
# DATABASE_URL=sqlite:///bookhive.db
